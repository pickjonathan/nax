---
description: "DE lifecycle ▸ Specify who your customer is (Theme 1, Steps 1–6)"
argument-hint: [venture-slug]
---

Use the **disciplined-entrepreneurship** skill (`references/theme-1-who-is-your-customer.md`) to work
Theme 1 for venture **$ARGUMENTS**, in order:

1 Market Segmentation → 2 Select a Beachhead → 3 End User Profile → 4 TAM → 5 Persona → 6 Full Life
Cycle Use Case.

- Delegate heavy lifting: `market-researcher` + `tam-estimator` for sizing (or `/size-market`);
  `customer-discovery-analyst` + `/personas` for the profile & persona.
- **Start customer interviews now** — inquiry, not advocacy — and log each in `interviews/`.
- Write findings into `01-who-is-your-customer.md`; route guesses to `assumptions.md`; mark steps
  **1–6** `[x]` as each becomes evidenced; update `dashboard.md` and `00-summary.md`.

**Gate ahead:** before planning you must validate the beachhead with ~10 real interviews — that's
**`/de-clarify`**. Finish by showing `/de-next <slug>`.
